/*
Create database View:
Use StudentDB database schema to create the following views.
1. Create a view using Student, Score table to identify all students whose score is >90. Show the results created by this view and take a screenshot.
2. Create a database view using Student.Score,Grade_Event table to identify all the students who have category of 'Q'. Show the results created by this view take a screenshot.
3. Submit the Queries used to create the view including the screenshot for the above views.
*/

CREATE VIEW view_name AS
SELECT column1, column2, ...
FROM table_name
WHERE condition;
